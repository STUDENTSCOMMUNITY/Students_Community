const sendOtp = (otp, mobileNo) => {
  console.log(`Sending OTP ${otp} to mobile number ${mobileNo}`);
  // Integrate with an actual SMS API service like Twilio or any other
};

module.exports = sendOtp;
